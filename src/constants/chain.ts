import type { Chain } from '../types'

/**
 * Specify the chains for token list generation.
 */
export const CHAINS: Chain[] = [
  {
    id: 'ethereum',
    name: 'ethereum',
    chainId: 1,
    assetId: 'ethereum',
    coingeckoId: 'ethereum',
  },
]
